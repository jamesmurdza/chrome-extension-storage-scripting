# Example Chrome Extension: Storage and Scripting

Features:
- Toolbar button which opens popup window
- Form in the popup window which saves values
- Button in the popup window which runs a script in the active tab
